query GetAnimeList($page: PositiveInt, $limit: PositiveInt, $order: OrderEnum) {
  animes(page: $page, limit: $limit, order: $order) {
    id
    name
    russian
    score
    poster {
    originalUrl
}
  }
}
